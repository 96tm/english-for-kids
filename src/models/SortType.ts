enum SortType {
  category = 'category',
  word = 'word',
  translation = 'translation',
  training = 'training',
  right = 'right',
  wrong = 'wrong',
  percentage = 'percentage',
}

export default SortType;
